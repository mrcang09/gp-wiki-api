# Tween功能

- Wiki Path: 通用功能 / Tween功能
- Article ID: 20351
- Source URL: https://developer.gp.qq.com/wikieditor/#/catalog/20351
- Mirrored At (UTC): 2026-05-19 08:23:14Z

---

## Source Content

# Tween 动画功能

Tween 动画系统提供了一套完整的差值平滑动画能力，支持位置、旋转、颜色等多种数值类型的平滑过渡，适用于UI动效、对象移动、场景变换等多种场景。

<br>

## 功能概述 
Tween 动画系统是一套基于时间差值的动画框架，通过在指定时间内将数值从起始值平滑过渡到目标值，配合多种缓动算法实现丰富的动画效果。

### 适用场景

Tween 动画系统特别适合以下场景：
+ UI 动效：按钮缩放、淡入淡出、颜色变化等界面动画
+ 对象移动：Actor 位置移动、旋转动画
+ 场景变换：摄像机平滑移动、场景元素过渡
+ 数值动画：进度条显示、数值计数动画
+ 视觉反馈：击中特效、状态变化反馈

---

### 核心优势
相较于传统的动画实现方式，Tween 动画系统具有以下优势：
+ 简单易用：只需指定起始值、目标值、持续时间和缓动类型即可创建动画
+ 灵活控制：支持暂停、恢复、停止、循环、往返等多种控制方式
+ 链式调用：支持多个动画串联执行，实现复杂的动画序列
+ 低学习成本：基于 Lua API 设计，开发者无需深入了解底层实现

<br>

## 功能详解

### 前置依赖

无前置依赖条件。Tween 动画系统作为 UGC 编辑器的内置功能提供，开发者可以直接在 Lua 脚本中调用相关 API。

---

### 整体简介
#### 功能背景
在游戏开发中，经常需要实现各种平滑过渡动画效果，例如 UI 元素的淡入淡出、按钮的缩放反馈、场景中物体的移动等。

传统的实现方式可能需要：手动编写复杂的插值算法、在 Tick 中逐帧更新数值、维护动画状态和生命周期。

Tween 动画系统提供了一种更简洁的解决方案：通过 API 直接创建动画，自动处理插值计算、内置多种缓动算法，只需选择类型即可、支持动画控制（暂停、恢复、停止、循环）、提供回调机制，在动画完成时执行后续逻辑。

#### 工作原理

Tween 动画系统的核心原理是将数值从起始值平滑过渡到目标值，通过缓动函数调整动画的节奏和加速度。

基本流程：
1. 定义数值范围：指定起始值和目标值
2. 设置动画参数：持续时间和缓动类型
3. 创建动画：通过 API 创建 Tween 动画
4. 自动执行：系统在每一帧自动计算当前值
5. 应用数值：通过回调函数将当前值应用到目标对象

<br>

## 详细配置

### 支持的动画类型
Tween 动画系统支持以下数值类型的动画：
|动画类型|API 函数|说明|适用场景|
|-|-|-|-|
|Actor 位置|TweenLibrary.TweenActorLocation|Actor 物体位置移动|角色移动、道具动画|
|Actor 旋转|TweenLibrary.TweenActorRotation|Actor 物体旋转动画|	角色转身、道具旋转|
|浮点数值|TweenLibrary.TweenFloatValue|浮点数值变化|进度条、数值显示|
|向量数值|TweenLibrary.TweenVectorValue|3D 向量变化|UI 元素移动、位置调整|
|旋转器数值|TweenLibrary.TweenRotatorValue|旋转器变化|角度控制、方向变化|
|颜色数值|TweenLibrary.TweenColorValue|颜色渐变|UI 颜色变化、特效颜色|

---

### 缓动类型（Easing Type）
缓动类型决定了动画的加速度和节奏，系统提供了 33 种缓动算法，对应 Tween.js 的缓动类型：

基础缓动：

|缓动类型|效果|使用场景|
|-|-|-|
|Linear|线性匀速|持续性动画、机械运动|
|QuadIn|二次加速|开始缓慢、逐渐加速|
|QuadOut|二次减速|开始快速、逐渐减速|
|QuadInOut|二次往返|平滑加速减速|
|CubicIn/Out/InOut|三次缓动|平滑过渡效果|
|QuartIn/Out/InOut|四次缓动|较强的加速/减速效果|

特殊缓动：

|缓动类型|效果|使用场景|
|-|-|-|
|SineIn/Out/InOut|正弦缓动|自然平滑的过渡|
|ExpoIn/Out/InOut|指数缓动|快速加速或减速|
|CircIn/Out/InOut|圆形缓动|圆周运动相关|
|ElasticIn/Out/InOut|弹性缓动|弹簧效果、回弹动画|
|BackIn/Out/InOut|超出回退|超出目标后返回|
|BounceIn/Out/InOut|弹跳缓动|弹跳效果、落地动画|

完整缓动类型列表：
``` 
Linear,
QuadIn, QuadOut, QuadInOut,
CubicIn, CubicOut, CubicInOut,
QuartIn, QuartOut, QuartInOut,
QuintIn, QuintOut, QuintInOut,
SineIn, SineOut, SineInOut,
ExpoIn, ExpoOut, ExpoInOut,
CircIn, CircOut, CircInOut,
ElasticIn, ElasticOut, ElasticInOut,
BackIn, BackOut, BackInOut,
BounceIn, BounceOut, BounceInOut
```

---

### 动画参数配置

Tween 动画支持以下配置参数，通过 UnrealTweenConfig 结构体设置：

``` Lua
local Config = CreateStruct('/Script/UnrealTween.UnrealTweenConfig')
```

参数说明：

|参数|类型|说明|默认值|
|-|-|-|-|
|Delay|float|初始延迟时间（秒）|0|
|RepeatCount|int32|重复次数（-1=无限，0和1=不重复）|1|
|bYoyo|bool|是否往返（A→B→A）|false|
|RepeatDelay|float|每次重复前的等待时间（秒）|0|

参数详细说明：
+ 延迟：动画执行前的等待时间。例如设置 Delay = 2.0，则动画会在 2 秒后才开始执行
+ 循环次数：
	RepeatCount = 0或者1：只播放一次
	RepeatCount = -1：无限循环
	RepeatCount = N：总共播放 N 次
+ 往返动画：开启后，动画会先从起始值到目标值，再从目标值回到起始值。例如设置 bYoyo = true，则动画路径为 A→B→A
+ 循环延迟：每次重复前的等待时间。只在重复次数 > 0 时生效

<br>

## 运行时使用方式

### 获取 TweenLibrary

在创建 Tween 动画之前，需要先获取 TweenLibrary，这是调用 Tween API 的核心库：

``` Lua
_G.TweenLibrary = KismetLibrary.New("/Script/UnrealTween.UnrealTweenBlueprintLibrary")
```

### 创建回调函数

Tween 动画通过回调函数来应用当前值到目标对象。回调函数会在动画每一帧执行，接收当前值作为参数。

**创建 UEDelegate 回调**：

``` Lua
local Delegate = UGCDelegateUtility.CreateUEDelegate(UGCGameSystem.GetLocalPlayerController())
Delegate:Bind(function(Obj, Value)
    -- Value 是当前动画的数值
    -- 在这里将 Value 应用到目标对象
    self.TargetWidget:SetColorAndOpacity(Value)
end, self)
```

### 创建 Tween 动画

以下是创建各种类型 Tween 动画的示例：

#### 颜色动画示例
``` Lua
function TweenColor:Construct()
    local Controller = UGCGameSystem.GetLocalPlayerController()
    
    -- 定义起始颜色和目标颜色
    local StartColor = KismetMathLibrary.MakeColor(0, 1, 0, 1)  -- 绿色
    local EndColor = KismetMathLibrary.MakeColor(1, 0, 0, 1)    -- 红色
    
    -- 创建回调函数
    local Delegate = UGCDelegateUtility.CreateUEDelegate(Controller)
    Delegate:Bind(function(Obj, Value)
        self.TweenTarget:SetColorAndOpacity(Value)
    end, self)
    
    -- 配置动画参数
    local Config = CreateStruct('/Script/UnrealTween.UnrealTweenConfig')
    Config.Delay = 0           -- 无延迟
    Config.RepeatCount = -1    -- 无限循环
    Config.bYoyo = false       -- 不往返
    Config.RepeatDelay = 0     -- 循环间无延迟
    
    -- 创建颜色动画（1秒持续时间，使用 Linear 缓动）
    self.TweenHandler = TweenLibrary.TweenColorValue(
        Controller,
        StartColor,
        EndColor,
        1.0,
        self.TweenType,        -- 缓动类型
        Delegate,
        Config
    )
    
    -- 暂停动画
    TweenLibrary.PauseTween(Controller, self.TweenHandler)
end
```

#### 移动动画示例

``` Lua
function TweenMove:Construct()
    local Controller = UGCGameSystem.GetLocalPlayerController()
    
    -- 定义起始位置和目标位置
    local StartLocation = KismetMathLibrary.MakeVector(0, 0, 0)
    local EndLocation = KismetMathLibrary.MakeVector(700, 0, 0)
    
    -- 创建回调函数
    local Delegate = UGCDelegateUtility.CreateUEDelegate(Controller)
    Delegate:Bind(function(Obj, Value)
        local Slot = UGCWidgetManagerSystem.SlotAsCanvasSlot(self.TweenImage)
        Slot:SetPosition(KismetMathLibrary.MakeVector2D(Value.X, 0))
    end, self)
    
    -- 配置动画参数
    local Config = CreateStruct('/Script/UnrealTween.UnrealTweenConfig')
    Config.Delay = 0
    Config.RepeatCount = -1
    Config.bYoyo = false
    Config.RepeatDelay = 0
    
    -- 创建向量动画（1秒持续时间）
    self.TweenHandler = TweenLibrary.TweenVectorValue(
        Controller,
        StartLocation,
        EndLocation,
        1.0,
        self.TweenType,
        Delegate,
        Config
    )
    
    -- 暂停动画
    TweenLibrary.PauseTween(Controller, self.TweenHandler)
end
```

#### Actor 位置动画示例

``` Lua
function MoveActor:ReceiveBeginPlay()
    local Controller = UGCGameSystem.GetLocalPlayerController()
    
    -- 定义目标位置
    local TargetLocation = KismetMathLibrary.MakeVector(100, 100, 100)
    
    -- 配置动画参数
    local Config = CreateStruct('/Script/UnrealTween.UnrealTweenConfig')
    Config.Delay = 0
    Config.RepeatCount = 0
    Config.bYoyo = false
    Config.RepeatDelay = 0
    
    -- 创建 Actor 位置动画
    local TweenHandler = TweenLibrary.TweenActorLocation(
        Controller,
        Actor,
        TargetLocation,
        2.0,
        EEasingType.EaseOutQuad,
        Config
    )
end
```
<br>

### 控制动画

Tween 动画提供了多种控制方式：

#### 暂停/恢复动画
``` Lua
function PauseCheckBox_OnCheckStateChanged(bIsChecked)
    if not self.TweenHandler then
        return
    end
    
    if bIsChecked then
        -- 暂停动画
        TweenLibrary.PauseTween(self.Controller, self.TweenHandler)
    else
        -- 恢复动画
        TweenLibrary.ResumeTween(self.Controller, self.TweenHandler)
    end
end
```

#### 修改动画配置（往返、循环）

``` Lua
function YoyoCheckBox_OnCheckStateChanged(bIsChecked)
    if not self.TweenHandler then
        return
    end
    
    -- 动态修改动画配置
    TweenLibrary.GetTweenSubsystem(self.Controller):ConfigureTween(
        self.TweenHandler,
        0,               -- Delay
        -1,              -- RepeatCount（-1 表示无限循环）
        bIsChecked,      -- bYoyo
        0                -- RepeatDelay
    )
end
```

#### 停止动画
``` Lua
function StopAnimation()
    -- 停止动画，TweenHandler会失效
    TweenLibrary.KillTween(Controller, TweenHandler)
end
```

#### 链式动画

将多个动画串联执行，当前一个动画完成时自动开始下一个动画：

``` Lua
function ChainAnimations()
    local Controller = UGCGameSystem.GetLocalPlayerController()
    
    -- 创建第一个动画
    local Handler1 = TweenLibrary.TweenColorValue(
        Controller,
        StartColor1,
        EndColor1,
        1.0,
        EEasingType.Linear,
        Delegate1,
        Config1
    )
    
    -- 创建第二个动画
    local Handler2 = TweenLibrary.TweenColorValue(
        Controller,
        StartColor2,
        EndColor2,
        1.0,
        EEasingType.EaseInQuad,
        Delegate2,
        Config2
    )
    
    -- 链接动画：Handler1 完成后自动执行 Handler2
    TweenLibrary.ChainTween(Controller, Handler1, Handler2)
end
```

#### 绑定完成回调

在动画完成时执行特定逻辑：

``` Lua
function BindCompletionCallback()
    -- 创建回调函数
    local CompletionDelegate = function()
        ugcprint("动画完成！")
        -- 执行后续逻辑
    end
    
    -- 绑定到 Tween
    TweenLibrary.BindCompletedDelegate(Controller, TweenHandler, CompletionDelegate)
end
```

#### 验证动画状态

检查动画是否仍然有效：
``` Lua
function CheckTweenState()
    local IsValid = TweenLibrary.IsTweenValid(Controller, TweenHandler)
    if IsValid then
        print("动画正在执行")
    else
        print("动画已停止")
    end
end
```

## API 参考

### TweenLibrary
功能说明：Tween 动画系统的核心 API 库，提供所有动画创建和控制函数。

+ **TweenColorValue**

	函数名称： ``TweenLibrary.TweenColorValue``

	功能说明： 创建颜色渐变动画

	参数说明：

	|参数|类型|说明|
	|-|-|-|
	|Context|UObject|函数上下文，推荐使用本地玩家控制器|
	|StartColor|FLinearColor|起始颜色|
	|EndColor|FLinearColor|目标颜色|
	|Duration|float|动画持续时间（秒）|
	|EasingType|EEasingType|缓动类型|
	|Delegate|FTweenColorDelegateBP|回调函数|
	|Config|FUnrealTweenConfig|动画配置|
	
	返回值：
	
	``FTweenHandle`` ：动画句柄，用于后续控制.
	
	使用示例：
	
	``` Lua
	local TweenHandler = TweenLibrary.TweenColorValue(
    Controller,
    KismetMathLibrary.MakeColor(0, 1, 0, 1),
    KismetMathLibrary.MakeColor(1, 0, 0, 1),
    1.0,
    EEasingType.Linear,
    Delegate,
    Config)
	```
	
+ **TweenVectorValue**

	函数名称： ``TweenLibrary.TweenVectorValue``

	功能说明： 创建向量数值动画

	参数说明：
	
	|参数|类型|说明|
	|-|-|-|
	|Context|UObject|函数上下文，推荐使用本地玩家控制器|
	|Start|FVector|起始向量|
	|End|FVector|目标向量|
	|Duration|float|动画持续时间（秒）|
	|EasingType|EEasingType|缓动类型|
	|Delegate|FTweenColorDelegateBP|回调函数|
	|Config|FUnrealTweenConfig|动画配置|
	
	返回值：

	``FTweenHandle`` ：动画句柄
	
+ **TweenFloatValue**

	函数名称： ``TweenLibrary.TweenFloatValue``

	功能说明： 创建浮点数值动画

	参数说明：
	
	|参数|类型|说明|
	|-|-|-|
	|Context|UObject|函数上下文，推荐使用本地玩家控制器|
	|Start|float|起始数值|
	|End|float|目标数值|
	|Duration|float|动画持续时间（秒）|
	|EasingType|EEasingType|缓动类型|
	|Delegate|FTweenColorDelegateBP|回调函数|
	|Config|FUnrealTweenConfig|动画配置|
	
	返回值：

	``FTweenHandle`` ：动画句柄
	
+ **TweenActorLocation**

	函数名称： TweenLibrary.TweenActorLocation

	功能说明： 创建 Actor 位置移动动画

	参数说明：
	
	|参数|类型|说明|
	|-|-|-|
	|Context|UObject|函数上下文，推荐使用本地玩家控制器|
	|Start|float|起始数值|
	|End|float|目标数值|
	|Duration|float|动画持续时间（秒）|
	|EasingType|EEasingType|缓动类型|
	|Delegate|FTweenColorDelegateBP|回调函数|
	|Config|FUnrealTweenConfig|动画配置|
	
	返回值：

	``FTweenHandle`` ：动画句柄
	
+ **PauseTween**

	函数名称： ``TweenLibrary.PauseTween``

	功能说明： 暂停动画执行

	参数说明：
	
	|参数|类型|说明|
	|-|-|-|
	|Context|UObject|函数上下文，推荐使用本地玩家控制器|
	|Handle|FTweenHandle|动画句柄|
	
	使用示例：
	``` Lua
	TweenLibrary.PauseTween(Controller, TweenHandler)
	```
	
+ **ResumeTween**

	函数名称： ``TweenLibrary.ResumeTween``

	功能说明： 恢复暂停的动画

	参数说明：

	|参数|类型|说明|
	|-|-|-|
	|Context|UObject|函数上下文，推荐使用本地玩家控制器|
	|Handle|FTweenHandle|动画句柄|
	
	使用示例：
	
	``` Lua
	TweenLibrary.ResumeTween(Controller, TweenHandler)
	```
	
+ **KillTween**

	函数名称： ``TweenLibrary.KillTween``

	功能说明： 停止动画并将对象恢复到起始值

	参数说明：

	|参数|类型|说明|
	|-|-|-|
	|Context|UObject|函数上下文，推荐使用本地玩家控制器|
	|Handle|FTweenHandle|动画句柄|
	
	使用示例：
	
	``` Lua
	TweenLibrary.KillTween(Controller, TweenHandler)
	```
	
+ **ChainTween** 
	
	函数名称： ``TweenLibrary.ChainTween``

	功能说明： 链接两个动画，当前一个动画完成时自动执行下一个

	参数说明：
	
	|参数|类型|说明|
	|-|-|-|
	|Context|UObject|函数上下文，推荐使用本地玩家控制器|
	|Handle|FTweenHandle|第一个动画的句柄|
	|NextHandle|FTweenHandle|第二个动画的句柄|
	
	使用示例：
	
	``` Lua
	TweenLibrary.ChainTween(Controller, Handler1, Handler2)
	```
	
+ **IsTweenValid**


	函数名称： ``TweenLibrary.IsTweenValid``

	功能说明： 检查动画是否仍然有效

	参数说明：
	
	|参数|类型|说明|
	|-|-|-|
	|Context|UObject|函数上下文，推荐使用本地玩家控制器|
	|Handle|FTweenHandle|动画句柄|
	
	返回值：

	``bool``：true 表示动画有效，false 表示动画已停止
	
+ **BindCompletedDelegate**

	函数名称： **TweenLibrary.BindCompletedDelegate**

	功能说明： 绑定动画完成回调函数

	参数说明：
	
	|参数|类型|说明|
	|-|-|-|
	|Context|UObject|函数上下文，推荐使用本地玩家控制器|
	|Handle|FTweenHandle|动画句柄|
	|Delegate|FTweenCompeleteBP|完成回调函数|
	
	使用示例：
	``` Lua
	TweenLibrary.BindCompletedDelegate(Controller, TweenHandler	, function()
    	ugcprint("动画完成！")
	end)
	```
